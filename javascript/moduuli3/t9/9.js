const btn = document.getElementById('start')
const result = document.getElementById('result')

btn.addEventListener('click', a=>{
    const calc = document.getElementById('calculation').value
    const nums = calc.split()
})
